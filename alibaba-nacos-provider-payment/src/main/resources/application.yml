server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#        server-addr: 192.168.0.132:1111 #换成nginx的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'